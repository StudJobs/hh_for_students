apiVersion: 1

providers:
  - name: 'StudJobs'
    orgId: 1
    folder: 'StudJobs'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
